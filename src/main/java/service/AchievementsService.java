package service;

import model.UserProgress;

public class AchievementsService {
    private final UserProgress userProgress;
    public AchievementsService(UserProgress userProgress) {
        this.userProgress = userProgress;
    }
}
